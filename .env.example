# Example environment variables for local development and deployment
# Do not commit real API keys or secrets. Use only safe defaults here.

LLM_ENABLED=false
LLM_PROVIDER=mock
LLM_MODEL=mock-v1
LLM_API_KEY=
LLM_BASE_URL=
LLM_TIMEOUT_SECONDS=20
LLM_MAX_TOKENS=500
